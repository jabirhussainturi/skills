# In progress

Drafts not yet ready to ship. These are **usable locally** (`scripts/link-skills.sh`
links them into `~/.claude/skills`), but they are **not** in `plugin.json` or the
top-level README, so they never install for anyone else.

A skill graduates from here via the promotion steps in [`../../CLAUDE.md`](../../CLAUDE.md).

- **[write-article](./write-article/SKILL.md)** — Turn source material into a multi-platform article set (Medium, LinkedIn, Dev.to, X thread, blog MDX) with Mermaid diagrams, written in your own voice. Destined for `personal/` once it survives a real end-to-end run.
